name: planqk-quantum
channels:
  - conda-forge
  - quantum-engineering
dependencies:
  - python=3.9
  - pip>=21.1.3
  - pip:
      # dev dependencies
      - qiskit
      - setuptools
      - pytest
      - pytest-cov
      - pytest-asyncio
      - busypie
      - retrying
      - python-dotenv
      # prod dependencies
      - requests==2.*
      ## aws braket
      - qiskit_braket_provider==0.0.4
      ## azure quantum
      - azure-quantum[qiskit]
      - qiskit-ionq
      ## dwave
      - dwave-ocean-sdk==6.4.1
